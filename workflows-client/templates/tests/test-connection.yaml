apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "workflows-client.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "workflows-client.name" . }}
    helm.sh/chart: {{ include "workflows-client.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "workflows-client.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
